{
  "name": "@synthex/openapi",
  "version": "0.0.1",
  "description": "Shared OpenAPI schemas, error responses, and base configuration for Synthex services",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "openapi3-ts": "^4.6.0"
  },
  "devDependencies": {
    "@synthex/config": "workspace:*",
    "typescript": "^5.3.0"
  }
}
